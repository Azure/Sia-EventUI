import { addFilter } from '../src/actions/filterActions'

describe('filterActions', () => {
    describe('addFilter', () => {
        describe('when oldFilter has eventTypes that does not include new event id', () => {
            
        })



        let mockHistory = []
        const mockOldFilter = {
            eventTypes: []
        }
        const mockEvent = {id: 22}


    })
})